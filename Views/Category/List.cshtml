@model AfaqMall.Models.Category

<h2>@Model.Name</h2>
<div class="row">
    @foreach (var product in Model.Products)
    {
        <div class="col-6 col-md-3 mb-3">
            <div class="card">
                <img src="@product.ImageUrl" class="card-img-top" />
                <div class="card-body">
                    <h5 class="card-title">@product.Name</h5>
                    <p>@product.Price $</p>
                    <p>⭐⭐⭐ (@product.Rating)</p>
                    <button class="btn btn-sm btn-outline-danger">❤️</button>
                    <a href="/Cart/AddToCart?categoryId=@Model.Id&productIndex=@Model.Products.IndexOf(product)" class="btn btn-sm btn-primary">🛒</a>
                </div>
            </div>
        </div>
    }
</div>

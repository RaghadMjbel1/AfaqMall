@model List<AfaqMall.Models.Category>

<h2>الأقسام</h2>
<div class="row mb-4">
    @foreach (var category in Model)
    {
        <div class="col-6 col-md-3 mb-3">
            <a href="/Category/List/@category.Id" class="btn btn-primary w-100">@category.Name</a>
        </div>
    }
</div>

<h2>منتجات متنوعة</h2>
<div class="row">
    @foreach (var category in Model)
    {
        foreach (var product in category.Products.Take(5))
        {
            <div class="col-6 col-md-3 mb-3">
                <div class="card">
                    <img src="@product.ImageUrl" class="card-img-top" />
                    <div class="card-body">
                        <h5 class="card-title">@product.Name</h5>
                        <p>@product.Price $</p>
                        <p>⭐⭐⭐ (@product.Rating)</p>
                        <button class="btn btn-sm btn-outline-danger">❤️</button>
                        <a href="/Cart/AddToCart?categoryId=@category.Id&productIndex=@category.Products.IndexOf(product)" class="btn btn-sm btn-primary">🛒</a>
                    </div>
                </div>
            </div>
        }
    }
</div>

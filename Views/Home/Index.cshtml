@model List<AfaqMall.Models.Category>

<h2>الأقسام</h2>
<div class="row mb-4">
    @foreach (var category in Model)
    {
        <div class="col-6 col-md-3 mb-3">
            <a href="/Category/List/@category.Id" class="btn btn-primary w-100">@category.Name</a>
        </div>
    }
</div>

<h2>منتجات متنوعة</h2>
<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
    @foreach (var category in Model)
    {
        foreach (var product in category.Products.Take(5))
        {
            <div class="col">
                <div class="card h-100">
                    <img src="@product.ImageUrl" class="card-img-top" />
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@product.Name</h5>
                        <p class="text-primary fw-bold">@product.Price $</p>
                        <p>⭐ @product.Rating</p>
                        <div class="mt-auto">
                            <button class="btn btn-outline-danger btn-sm">❤️</button>
                            <a href="/Cart/AddToCart?categoryId=@category.Id&productIndex=@category.Products.IndexOf(product)" class="btn btn-primary btn-sm">🛒</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    }
</div>
